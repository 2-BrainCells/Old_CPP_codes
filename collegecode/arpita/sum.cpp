#include <iostream>
#include <string>

using namespace std;
int main()
{
    float a = 5.343, b = 6;
    // cout << (a & b) << endl;
    // cout << (a | b) << endl;
    // cout << (a ^ b) << endl;
    // cout << (~a) << endl;

    cout << int(a);
    string s;
    getline(cin >> ws, s);
    return 0;
}

// conrol statements, loops, pointer, array , string , function,  
// ++ , -- , += , -= , && , || , !, &