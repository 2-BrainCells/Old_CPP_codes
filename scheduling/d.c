
#include <stdio.h>
#include <stdlib.h>

struct Process
{
    int pid; // process ID
    int at;  // arrival time
    int bt;  // burst time
    int ct;
    int wt;  // waiting time
    int tat; // turnaround time
    int rt;  // remaining time
};

void enter(struct Processes ps[], int n)
{
    printf("Enter the pid for the process = ");
    scanf("%d", &ps[i].pid)
}
int main()
{
    return 0;
}